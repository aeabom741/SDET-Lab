[pytest]
# 把 repo 根目錄放進 import path，讓 test 能 import 共用框架 api/ 與 conf/。
# 沒有這行時，只有 `python -m pytest`（會自動加 cwd）能跑，
# 而 `pytest` / `uv run pytest`（console script）會 ModuleNotFoundError。
pythonpath = .
addopts = --alluredir=allure-results --clean-alluredir